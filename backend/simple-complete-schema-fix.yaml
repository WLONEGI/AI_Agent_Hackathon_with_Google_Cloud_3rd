steps:
  - name: 'python:3.11-slim'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        echo "🔧 Installing dependencies for complete schema fix..."
        pip install --upgrade pip
        pip install asyncpg google-cloud-sql-connector sqlalchemy[asyncio]

        echo "📋 Running complete schema fix v2 via Python script..."
        python run_complete_schema_fix.py

        echo "🎉 Complete schema fix v2 finished!"

timeout: 600s

options:
  logging: CLOUD_LOGGING_ONLY